{
  "name": "osaka-rail-3d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir false --base ./",
    "preview": "vite preview",
    "serve-dist": "node scripts/serve-dist.js",
    "build-data": "node scripts/build-data.js",
    "test": "vitest run"
  },
  "dependencies": {
    "japan-train-data": "^0.6.0",
    "three": "^0.185.1",
    "vite": "^8.1.3",
    "vitest": "^4.1.10"
  }
}
